<aside class="sidebar-left">
    <!-- Trip Switcher -->
    <!-- <form method="GET" action="/viajes" style="margin-bottom: 16px;">
        <select name="trip_id" onchange="this.form.submit()">
            <% trips.forEach(t => { %>
            <option value="<%= t.id %>" <%= t.id === trip.id ? 'selected' : '' %>>
                <%= t.name %>
            </option>
            <% }) %>
        </select>
    </form> -->

    <!-- Everything related to creating a new post -->
    <!-- <div id="post-controls">
        <button class="button showPostForm" style="margin-bottom: 20px;">+ Nuevo</button>
    </div> -->

    <!-- Quick Filters -->
    <div class="sidebar-links">
        <a href="#">📸 Media</a><br>
        <a href="#">❤️ Liked</a><br>
        <a href="#">📝 My Posts</a>
    </div>
</aside>