<% if (tripMode === 'current' || tripMode === 'upcoming') { %>

<%- include('feed/current-upcoming',{ trips, trip, posts, theresMore, POSTS_PER_PAGE }) %>

<% } else { %>

<%- include('feed/past-trips', { trips, trip, posts, theresMore, POSTS_PER_PAGE }) %>

<% } %>