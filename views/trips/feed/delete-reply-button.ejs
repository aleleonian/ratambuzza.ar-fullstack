<button hx-delete="/trips/<%= trip.slug %>/feed/<%= post.id %>/replies/<%= reply.id %>" hx-target="#replies-section"
    hx-swap="innerHTML" hx-confirm="Seguro de borrar esta respuesta?" title="Borrar" class="button danger">
    <img src="/images/trash.png" alt="Respuestas" style="width: 25px;">
</button>