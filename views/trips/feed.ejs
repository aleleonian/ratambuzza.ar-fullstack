<% if (tripMode === 'current' || tripMode === 'upcoming') { %>
moreUrl:
<%- moreUrl %>
<%- include('feed/current-upcoming',{ trips, trip, posts, moreUrl, POSTS_PER_PAGE }) %>

<% } else { %>

<%- include('feed/past-trips', { trips, trip, posts, moreUrl, POSTS_PER_PAGE }) %>

<% } %>